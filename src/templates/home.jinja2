{% extends "base.jinja2" %}
{% block content %}
    <div class="jumbotron">
        <h1>Pricing Service</h1>
        <p>We scan online stores websites to alert you of price drops in items that interest you.</p>
        {% if not session['email'] %}
            <p>Just sign up, and get started by adding the url of a nitem that you wish to track. We'll do the rest!</p>
            <p><a class="btn btn-primary btn-lg" href="{{ url_for('users.register_user') }}" role="button">Sign up</a></5p>
        {% else %}
            <p>Welcome, {{ session['email'] }}.<br />Start Pricing Service right now!</p>
        {% endif %}
    </div>
{% endblock %}