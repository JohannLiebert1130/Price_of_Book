{% extends "base.jinja2" %}
{% block content %}
    <div class="page-header">
        <h1>Create Store</h1>
    </div>

    <div class="form">
        <form method="post">
            <div class="form-group">
                <label for="name">Store name</label>
                <input type="text" class="form-control" id="name" name="name" placeholder="Store Name"
                       value="{{ store.name }}">
            </div>
            <div class="form-group">
                <label for="url_prefix">Store URL prefix</label>
                <input type="text" class="form-control" id="url_prefix" name="url_prefix" value="{{ store.url_prefix }}"
                       aria-describedby="storeURLPrefixHelp">
                <span id="storeURLPrefixHelp" class="help-block">What characters do URLs of all items in this online store start with?</span>
            </div>

            <div class="form-group">
                <label for="price_tag_name">Price Tag name</label>
                <input type="text" class="form-control" id="price_tag_name" name="price_tag_name" placeholder="price_tag_name"
                       value="{{ store.price_tag_name }}"
                       aria-describedby="storePriceTagNameHelp">
                <span id="storePriceTagNameHelp" class="help-block">What is the HTML tag of the item that contains the item price? (usually <code>span</code>)</span>
            </div>

            <div class="form-group">
                <label for="price_query">Search Price Query</label>
                <input type="text" class="form-control" id="price_query" name="price_query" placeholder='{"id" : "price-now"}'
                       value='{{ store.price_query | tojson }}'
                       aria-describedby="PriceQueryHelpBlock">
                <span id="PriceQueryHelpBlock" class="help-block">What JSON query would we have to do to find the specific tag with price? Remember to use
                double quotation marks! </span>
            </div>

            <div class="form-group">
                <label for="image_tag_name">Image Tag name</label>
                <input type="text" class="form-control" id="image_tag_name" name="image_tag_name" placeholder="image_tag_name"
                       value="{{ store.image_tag_name }}"
                       aria-describedby="StoreImageTagNameHelp">
                <span id="StoreImageTagNameHelp" class="help-block">What is the HTML tag of the item that contains the item image?</span>
            </div>

            <div class="form-group">
                <label for="image_query">Search Image Query</label>
                <input type="text" class="form-control" id="image_query" name="image_query" value='{{ store.image_query | tojson }}'
                       aria-describedby="ImageQueryHelpBlock">
                <span id="ImageQueryHelpBlock" class="help-block">What JSON query would we have to do to find the specific tag with image? Remember to use
                double quotation marks! </span>
            </div>


            <button type="submit" class="btn btn-warning">Update</button>
        </form>
    </div>
{% endblock %}